/* ══════════════════════════════════════════════════════════════════════════
   RocketSimStyles.uss  —  Right Config Panel
   Fixes: TextField text invisible, EnumField PPO/SAC text invisible,
          text overflowing boxes, per-tab colour themes, general polish.
   ══════════════════════════════════════════════════════════════════════════ */

/* ── Panel shell ─────────────────────────────────────────────────────────── */
.rs-right-panel {
    position: absolute;
    top: 14px;
    bottom: 14px;
    width: 460px;
    background-color: rgba(9, 12, 20, 0.96);
    border-top-left-radius: 14px;
    border-bottom-left-radius: 14px;
    border-left-width: 1px;
    border-top-width: 1px;
    border-bottom-width: 1px;
    border-color: rgba(70, 130, 255, 0.28);
    flex-direction: column;
    overflow: hidden;
}

/* ── Slide tab ───────────────────────────────────────────────────────────── */
.rs-right-tab {
    position: absolute;
    top: 50%;
    width: 20px;
    height: 52px;
    translate: 0 -26px;
    background-color: rgba(9, 12, 20, 0.96);
    border-top-left-radius: 8px;
    border-bottom-left-radius: 8px;
    border-left-width: 1px;
    border-top-width: 1px;
    border-bottom-width: 1px;
    border-right-width: 0;
    border-color: rgba(70, 130, 255, 0.28);
    color: rgba(140, 180, 255, 1);
    font-size: 14px;
    padding: 0;
    align-items: center;
    justify-content: center;
}
.rs-right-tab:hover { background-color: rgba(30, 55, 140, 0.65); }

.rs-panel-resize-handle {
    position: absolute;
    left: -4px;
    top: 0;
    bottom: 0;
    width: 11px;
}

.rs-footer-mode-btn {
    width: 76px;
    height: 30px;
    margin-right: 3px;
    background-color: rgb(28, 42, 68);
    border-width: 1px;
    border-color: rgba(105, 145, 205, 0.72);
    color: rgb(180, 205, 238);
    font-size: 10px;
}
.rs-mode-btn-active { background-color: rgb(30, 92, 195); border-color: rgb(90, 165, 255); color: rgb(235, 245, 255); }

.rs-panel-footer {
    min-height: 46px;
    padding: 8px 10px 9px;
    background-color: rgb(15, 25, 47);
    border-top-width: 2px;
    border-color: rgba(75, 145, 255, 0.78);
}
.rs-panel-notification { color: rgb(145, 190, 235); font-size: 10px; white-space: normal; margin-bottom: 5px; }
.rs-panel-notification-error { color: rgb(255, 125, 105); }
.rs-footer-actions { flex-direction: row; min-height: 30px; }
.rs-footer-actions .rs-action-btn { flex-grow: 1; margin: 0; background-color: rgb(27, 82, 185); border-width: 2px; border-color: rgb(78, 150, 255); color: rgb(235, 245, 255); }
.rs-footer-actions .rs-danger-btn { flex-grow: 1; height: 30px; margin: 0; }

/* ══ TAB BAR ════════════════════════════════════════════════════════════════ */
.rs-tab-bar {
    flex-direction: row;
    flex-wrap: nowrap;
    height: 34px;
    min-height: 34px;
    padding: 4px 4px 3px;
    border-bottom-width: 1px;
    border-color: rgba(70, 130, 255, 0.14);
}

.rs-tab-btn {
    width: 12.5%;
    flex-grow: 0;
    flex-shrink: 1;
    height: 26px;
    background-color: transparent;
    border-width: 0;
    border-radius: 5px;
    color: rgba(120, 150, 190, 1);
    font-size: 10px;
    padding: 0;
    overflow: hidden;
    -unity-text-align: middle-center;
}
.rs-tab-btn:hover { background-color: rgba(35, 60, 140, 0.30); }

/* ── Per-tab active colours (button) ────────────────────────────────────── */
.rs-theme-parts.rs-tab-btn-active      { background-color: rgba(20, 65, 175, 0.50); color: rgb(110, 195, 255); -unity-font-style: bold; }
.rs-theme-telemetry.rs-tab-btn-active  { background-color: rgba(75, 20, 175, 0.50); color: rgb(195, 120, 255); -unity-font-style: bold; }
.rs-theme-env.rs-tab-btn-active        { background-color: rgba(15, 120, 80,  0.50); color: rgb(70,  210, 155); -unity-font-style: bold; }
.rs-theme-scenario.rs-tab-btn-active   { background-color: rgba(160, 100, 10, 0.50); color: rgb(255, 190, 70);  -unity-font-style: bold; }
.rs-theme-ml.rs-tab-btn-active         { background-color: rgba(165, 15, 75,  0.50); color: rgb(255, 110, 165); -unity-font-style: bold; }
.rs-theme-rewards.rs-tab-btn-active    { background-color: rgba(125, 55, 10, 0.55); color: rgb(255, 170, 85); -unity-font-style: bold; }
.rs-theme-run.rs-tab-btn-active        { background-color: rgba(35, 80, 145, 0.55); color: rgb(120, 195, 255); -unity-font-style: bold; }
.rs-theme-faults.rs-tab-btn-active     { background-color: rgba(155, 45, 25, 0.55); color: rgb(255, 135, 105); -unity-font-style: bold; }

/* ══ CONTENT AREA ═══════════════════════════════════════════════════════════ */
.rs-panel-content {
    padding: 5px 10px 12px;
    flex-direction: column;
}

/* ── Section labels — base ───────────────────────────────────────────────── */
.rs-section-label {
    font-size: 9px;
    -unity-font-style: bold;
    letter-spacing: 1.2px;
    margin-top: 14px;
    margin-bottom: 5px;
    padding-bottom: 3px;
    border-bottom-width: 1px;
    /* fallback colour; overridden per-tab below */
    color: rgba(100, 150, 255, 0.85);
    border-color: rgba(70, 130, 255, 0.14);
}

/* ── Per-tab section label accent colours ────────────────────────────────── */
.rs-theme-parts      .rs-section-label { color: rgba(80, 185, 255, 0.90);  border-color: rgba(70, 160, 255, 0.18); }
.rs-theme-telemetry  .rs-section-label { color: rgba(185, 105, 255, 0.90); border-color: rgba(155, 75, 255, 0.18); }
.rs-theme-env        .rs-section-label { color: rgba(60, 205, 145, 0.90);  border-color: rgba(45, 180, 120, 0.18); }
.rs-theme-scenario   .rs-section-label { color: rgba(255, 185, 60, 0.90);  border-color: rgba(220, 155, 40, 0.18); }
.rs-theme-ml         .rs-section-label { color: rgba(255, 100, 155, 0.90); border-color: rgba(220, 75,  130, 0.18); }
.rs-theme-rewards    .rs-section-label { color: rgba(255, 165, 75, 0.90); border-color: rgba(220, 120, 45, 0.18); }
.rs-theme-run        .rs-section-label { color: rgba(105, 190, 255, 0.90); border-color: rgba(65, 145, 220, 0.18); }
.rs-theme-faults     .rs-section-label { color: rgba(255, 125, 95, 0.90); border-color: rgba(225, 80, 55, 0.18); }

.rs-foldout { margin-top: 4px; margin-bottom: 4px; }
.rs-foldout .unity-foldout__toggle,
.rs-foldout .unity-foldout__toggle:checked,
.rs-foldout .unity-foldout__text,
.rs-foldout .unity-toggle__label {
    color: rgb(170, 200, 235);
}
.rs-foldout-hardware-disabled .unity-foldout__toggle,
.rs-foldout-hardware-disabled .unity-foldout__text,
.rs-foldout-hardware-disabled .unity-toggle__label {
    color: rgb(82, 102, 128);
}

/* ── Field rows ──────────────────────────────────────────────────────────── */
.rs-field-row {
    flex-direction: row;
    align-items: center;
    min-height: 28px;
    margin-bottom: 5px;
    padding: 3px 5px;
    background-color: rgba(16, 23, 39, 0.48);
    border-bottom-width: 1px;
    border-bottom-color: rgba(90, 125, 175, 0.22);
    border-radius: 4px;
}
.rs-field-label {
    color: rgb(178, 202, 230);
    font-size: 9px;
    width: 150px;
    flex-shrink: 0;
    overflow: visible;
    white-space: normal;
}
.rs-slider         { flex-grow: 1; margin-left: 4px; margin-right: 4px; }
.rs-field-value    { color: rgb(80, 200, 120); font-size: 10px; width: 62px; -unity-text-align: middle-right; flex-shrink: 0; }
.rs-field-readonly { color: rgb(145, 195, 150); font-size: 10px; flex-grow: 1; -unity-text-align: middle-right; overflow: hidden; white-space: normal; }
.rs-toggle         { margin-left: auto; flex-shrink: 0; }
.rs-described-control {
    margin-bottom: 5px;
    padding: 4px 5px 5px;
    background-color: rgba(16, 23, 39, 0.48);
    border-bottom-width: 1px;
    border-bottom-color: rgba(90, 125, 175, 0.22);
    border-radius: 4px;
}
.rs-described-control > .rs-field-row {
    min-height: 22px;
    margin-bottom: 1px;
    padding: 0;
    background-color: transparent;
    border-width: 0;
}
.rs-control-description {
    color: rgb(132, 156, 187);
    font-size: 9px;
    margin-left: 154px;
    margin-right: 62px;
    margin-bottom: 3px;
    white-space: normal;
}
.rs-group-detail {
    color: rgb(132, 156, 187);
    font-size: 10px;
    margin-left: 20px;
    margin-bottom: 4px;
    white-space: normal;
}
.rs-ml-detail {
    font-size: 9px;
    margin-left: 154px;
    margin-right: 62px;
    margin-bottom: 3px;
}

/* Keyboard-editable numeric slider. The classes intentionally separate sign
   and parameter category so reward, event, terminal, and threshold rows can be
   themed independently without changing the control implementation. */
.rs-numeric-slider-control {
    border-left-width: 2px;
    border-left-color: rgba(90, 115, 150, 0.20);
    padding-left: 3px;
}
.rs-numeric-slider-row { min-height: 26px; }
.rs-numeric-slider-label { width: 118px; }
.rs-numeric-sign {
    width: 16px;
    min-width: 16px;
    flex-shrink: 0;
    font-size: 12px;
    -unity-font-style: bold;
    -unity-text-align: middle-center;
}
.rs-numeric-sign-positive { color: rgb(80, 205, 125); }
.rs-numeric-sign-negative { color: rgb(240, 105, 95); }
.rs-numeric-sign-neutral  { color: rgb(105, 130, 165); }
.rs-numeric-slider { min-width: 55px; }
.rs-numeric-input {
    width: 80px;
    min-width: 80px;
    height: 22px;
    margin: 0 0 0 3px;
    flex-shrink: 0;
    color: rgb(205, 225, 250);
    --unity-cursor-color: rgb(125, 185, 255);
    --unity-selection-color: rgba(50, 115, 210, 0.55);
}
.rs-numeric-input > .unity-base-field__input,
.rs-numeric-input .unity-base-text-field__input {
    background-color: rgba(10, 15, 32, 0.90);
    border-width: 1px;
    border-color: rgba(80, 125, 190, 0.42);
    border-radius: 4px;
    padding: 0 5px;
    color: rgb(205, 225, 250);
}
.rs-numeric-input > .unity-base-field__input:focus,
.rs-numeric-input .unity-base-text-field__input:focus {
    border-color: rgba(105, 165, 255, 0.78);
}
.rs-numeric-input .unity-text-element,
.rs-numeric-input .unity-base-text-field__input > .unity-text-element,
.rs-numeric-input .unity-text-element--inner-input-field-component {
    color: rgb(225, 238, 255);
    font-size: 10px;
    -unity-text-align: middle-right;
}
.rs-numeric-input-corrected > .unity-base-field__input,
.rs-numeric-input-corrected .unity-base-text-field__input {
    border-color: rgba(255, 170, 65, 0.85);
}
.rs-numeric-unit {
    width: 44px;
    min-width: 44px;
    margin-left: 3px;
    flex-shrink: 0;
    color: rgb(115, 145, 180);
    font-size: 9px;
    overflow: hidden;
    white-space: nowrap;
}
.rs-numeric-unit-empty { opacity: 0; }
.rs-numeric-reset {
    width: 36px;
    min-width: 36px;
    height: 21px;
    margin-left: 3px;
    padding-left: 2px;
    padding-right: 2px;
    flex-shrink: 0;
    background-color: rgba(28, 38, 58, 0.82);
    border-width: 1px;
    border-color: rgba(90, 120, 165, 0.34);
    border-radius: 4px;
    color: rgb(140, 165, 198);
    font-size: 8px;
}
.rs-numeric-reset:hover { border-color: rgba(120, 165, 225, 0.60); }
.rs-numeric-slider-control > .rs-control-description {
    margin-left: 137px;
    margin-right: 163px;
}
.rs-numeric-category-event {
    border-left-color: rgba(90, 155, 235, 0.55);
}
.rs-numeric-category-terminal {
    border-left-color: rgba(215, 140, 60, 0.65);
}
.rs-numeric-category-threshold {
    border-left-color: rgba(140, 115, 220, 0.55);
}

/* ── Section description (path display, etc.) ────────────────────────────── */
.rs-section-desc {
    color: rgb(130, 157, 190);
    font-size: 10px;
    white-space: normal;
    overflow: hidden;
}

/* ══ ENUM FIELDS — Fix invisible PPO / SAC text ══════════════════════════════
   Unity UIToolkit EnumField structure:
     EnumField > .unity-base-field__input (.unity-enum-field__input)
                     > .unity-enum-field__text
                     > .unity-enum-field__arrow
   The popup is rendered into a separate overlay layer but also picks up the
   document's stylesheet, so .unity-base-dropdown__* rules apply.           */

.rs-enum-field {
    min-height: 30px;
    margin-bottom: 7px;
    padding: 4px 5px;
    background-color: rgba(16, 23, 39, 0.48);
    border-bottom-width: 1px;
    border-bottom-color: rgba(90, 125, 175, 0.22);
    border-radius: 4px;
    color: rgb(165, 190, 215);
    font-size: 11px;
}

/* Label part of the enum row */
.rs-enum-field > .unity-base-field__label {
    color: rgb(178, 202, 230);
    font-size: 11px;
    min-width: 118px;
    flex-shrink: 0;
    -unity-text-overflow-position: end;
    overflow: hidden;
}

/* The clickable input box */
.rs-enum-field > .unity-base-field__input,
.rs-enum-field .unity-enum-field__input {
    background-color: rgba(12, 18, 38, 0.85);
    border-width: 1px;
    border-color: rgba(70, 130, 255, 0.35);
    border-radius: 4px;
    flex-grow: 1;
    height: 22px;
    padding: 0 6px;
}
.rs-enum-field > .unity-base-field__input:hover,
.rs-enum-field .unity-enum-field__input:hover {
    border-color: rgba(100, 160, 255, 0.60);
}

/* The selected value text — this was invisible before */
.rs-enum-field .unity-enum-field__text,
.rs-enum-field .unity-base-popup-field__text {
    color: rgb(195, 220, 255);
    font-size: 11px;
    -unity-text-align: middle-left;
}

/* Dropdown arrow */
.rs-enum-field .unity-enum-field__arrow {
    -unity-background-image-tint-color: rgba(140, 175, 230, 0.80);
}
.rs-enum-field .unity-base-popup-field__arrow {
    -unity-background-image-tint-color: rgba(140, 175, 230, 0.80);
}
.rs-initialize-from-field {
    margin-top: 3px;
}

/* Disabled parameters remain clearly legible while their controls still look
   inactive. Unity's default disabled opacity was too low against this panel. */
.rs-field-row.unity-disabled,
.rs-field-row:disabled,
.rs-described-control.unity-disabled,
.rs-described-control:disabled,
.rs-enum-field.unity-disabled,
.rs-enum-field:disabled {
    opacity: 0.78;
}
.rs-action-btn.unity-disabled,
.rs-action-btn:disabled,
.rs-footer-mode-btn.unity-disabled,
.rs-footer-mode-btn:disabled {
    opacity: 0.72;
}

/* ── Popup dropdown overlay (EnumField options list) ─────────────────────── */
.unity-base-dropdown__container-outer,
.unity-base-dropdown__container-inner {
    background-color: rgba(10, 14, 28, 0.98);
    border-width: 1px;
    border-color: rgba(70, 130, 255, 0.40);
    border-radius: 5px;
}

.unity-base-dropdown__item {
    background-color: transparent;
    border-radius: 3px;
    margin: 1px 3px;
    padding: 2px 6px;
    min-height: 22px;
}
.unity-base-dropdown__item:hover {
    background-color: rgba(35, 70, 170, 0.45);
}
/* Item label — the actual text (was invisible / white-on-white) */
.unity-base-dropdown__item-label {
    color: rgb(195, 220, 255);
    font-size: 11px;
}
/* Checked item */
.unity-base-dropdown__item.unity-checked > .unity-base-dropdown__item-label {
    color: rgb(130, 210, 255);
    -unity-font-style: bold;
}
.unity-base-dropdown__checkmark {
    -unity-background-image-tint-color: rgb(90, 185, 255);
    width: 12px;
    height: 12px;
}
/* Separator */
.unity-base-dropdown__separator {
    background-color: rgba(70, 130, 255, 0.15);
    height: 1px;
    margin: 2px 6px;
}

/* ══ RUN ID TEXT FIELD — Fix invisible typed text ════════════════════════════
   Unity TextField structure:
     TextField > .unity-base-field__input (.unity-base-text-field__input)
                     > .unity-text-element (cursor + typed text)             */

.rs-run-id-field {
    flex-grow: 1;
    height: 22px;
    font-size: 11px;
    margin-left: 4px;
}

/* The actual input box */
.rs-run-id-field > .unity-base-field__input,
.rs-run-id-field .unity-base-text-field__input {
    background-color: rgba(10, 15, 32, 0.85);
    border-width: 1px;
    border-color: rgba(70, 130, 255, 0.38);
    border-radius: 4px;
    padding: 0 5px;
    color: rgb(200, 220, 255);
}
.rs-run-id-field > .unity-base-field__input:focus,
.rs-run-id-field .unity-base-text-field__input:focus {
    border-color: rgba(90, 155, 255, 0.70);
}

/* The text element that actually renders typed characters */
.rs-run-id-field .unity-text-element,
.rs-run-id-field .unity-base-text-field__input > .unity-text-element {
    color: rgb(200, 220, 255);
    font-size: 11px;
    -unity-text-align: middle-left;
}

/* Cursor */
.rs-run-id-field .unity-text-element--inner-input-field-component {
    color: rgb(200, 220, 255);
}

/* ══ RUN AUTOCOMPLETE DROPDOWN ══════════════════════════════════════════════ */
.rs-run-dropdown {
    background-color: rgba(10, 14, 28, 0.98);
    border-width: 1px;
    border-color: rgba(70, 130, 255, 0.38);
    border-radius: 5px;
    margin-bottom: 4px;
    overflow: hidden;
}

.rs-run-item {
    flex-direction: row;
    align-items: center;
    padding: 4px 8px;
    border-bottom-width: 1px;
    border-color: rgba(70, 130, 255, 0.08);
    min-height: 26px;
}
.rs-run-item:hover { background-color: rgba(35, 70, 170, 0.38); }

.rs-run-item-name {
    color: rgb(185, 210, 255);
    font-size: 11px;
    flex-grow: 1;
    overflow: hidden;
    white-space: nowrap;
}
.rs-run-item-meta {
    color: rgb(90, 130, 170);
    font-size: 10px;
    margin-left: 8px;
    flex-shrink: 0;
}

/* ══ VALIDATION HINT ════════════════════════════════════════════════════════ */
.rs-validation-label {
    font-size: 10px;
    margin-left: 6px;
    margin-bottom: 4px;
    white-space: normal;
}

/* ══ LOADED-CONFIG BANNER ═══════════════════════════════════════════════════ */
.rs-info-banner {
    flex-direction: row;
    align-items: center;
    background-color: rgba(14, 60, 32, 0.65);
    border-width: 1px;
    border-color: rgba(50, 170, 80, 0.45);
    border-radius: 5px;
    padding: 5px 8px;
    margin-bottom: 6px;
}
.rs-banner-label {
    color: rgb(105, 210, 130);
    font-size: 10px;
    flex-grow: 1;
    white-space: normal;
    overflow: hidden;
}
.rs-banner-btn {
    background-color: rgba(22, 90, 45, 0.55);
    border-color: rgba(60, 170, 90, 0.55);
    border-width: 1px;
    border-radius: 4px;
    color: rgb(140, 230, 160);
    height: 20px;
    font-size: 10px;
    padding: 0 6px;
    margin-left: 6px;
    flex-shrink: 0;
}
.rs-banner-btn:hover { background-color: rgba(38, 125, 65, 0.75); }

/* ══ ACTION BUTTONS ═════════════════════════════════════════════════════════ */
.rs-action-btn {
    background-color: rgba(22, 80, 200, 0.40);
    border-color: rgba(70, 140, 255, 0.65);
    border-width: 1px;
    border-radius: 7px;
    color: rgb(190, 215, 255);
    height: 30px;
    font-size: 11px;
    margin-bottom: 5px;
}
.rs-action-btn:hover { background-color: rgba(38, 112, 255, 0.58); }

.rs-danger-btn {
    background-color: rgba(140, 24, 24, 0.35);
    border-color: rgba(215, 65, 65, 0.55);
    border-width: 1px;
    border-radius: 7px;
    color: rgb(255, 145, 145);
    height: 27px;
    font-size: 11px;
    margin-top: 12px;
}
.rs-danger-btn:hover { background-color: rgba(190, 44, 44, 0.60); }

/* Hardware test controls */
.rs-test-wrapper {
    position: absolute;
    left: 14px;
    bottom: 14px;
    width: 220px;
    flex-direction: column-reverse;
}
.rs-test-main-btn {
    height: 32px;
}
.rs-test-options {
    flex-direction: column;
    background-color: rgba(12, 18, 34, 0.78);
    border-width: 1px;
    border-color: rgba(70, 130, 255, 0.22);
    border-radius: 7px;
    padding: 6px;
    margin-bottom: 6px;
}
.rs-test-status {
    color: rgb(150, 180, 215);
    font-size: 10px;
    white-space: normal;
    margin-top: 4px;
}

/* ══ ENGINE SELECTOR ════════════════════════════════════════════════════════ */
.rs-engine-row {
    flex-direction: row;
    justify-content: space-between;
    margin-top: 4px;
    margin-bottom: 8px;
}
.rs-engine-card {
    width: 86px;
    padding: 5px;
    background-color: rgba(18, 24, 42, 0.72);
    border-width: 1px;
    border-color: rgba(70, 130, 255, 0.20);
    border-radius: 8px;
    align-items: center;
    overflow: hidden;
}
.rs-engine-card:hover        { border-color: rgba(90, 150, 255, 0.50); }
.rs-engine-card-active {
    background-color: rgba(22, 55, 155, 0.52);
    border-color: rgba(90, 155, 255, 0.82);
}
.rs-engine-img {
    width: 62px;
    height: 62px;
    margin-bottom: 3px;
    -unity-background-image-tint-color: rgba(160, 200, 255, 0.90);
}
.rs-engine-label {
    color: rgb(175, 205, 255);
    font-size: 11px;
    -unity-font-style: bold;
    -unity-text-align: middle-center;
    overflow: hidden;
    white-space: nowrap;
}

/* ══ FIN SELECTOR ═══════════════════════════════════════════════════════════ */
.rs-fin-row {
    flex-direction: row;
    justify-content: space-between;
    margin-top: 4px;
    margin-bottom: 8px;
}
.rs-fin-card {
    width: 86px;
    padding: 5px;
    background-color: rgba(18, 24, 42, 0.72);
    border-width: 1px;
    border-color: rgba(70, 130, 255, 0.20);
    border-radius: 8px;
    align-items: center;
    overflow: hidden;
}
.rs-fin-card:hover     { border-color: rgba(90, 150, 255, 0.50); }
.rs-fin-card-active {
    background-color: rgba(22, 55, 155, 0.52);
    border-color: rgba(90, 155, 255, 0.82);
}
.rs-fin-img {
    width: 62px;
    height: 62px;
    margin-bottom: 3px;
    -unity-background-image-tint-color: rgba(160, 200, 255, 0.90);
}
.rs-fin-label {
    color: rgb(175, 205, 255);
    font-size: 11px;
    -unity-font-style: bold;
    -unity-text-align: middle-center;
    overflow: hidden;
    white-space: nowrap;
}

/* ══ HARDWARE PRESETS / BURN GROUPS ════════════════════════════════════════ */
.rs-preset-list {
    margin-top: 4px;
    margin-bottom: 8px;
}
.rs-preset-btn {
    flex-direction: column;
    align-items: stretch;
    padding: 6px;
    margin-bottom: 5px;
    background-color: rgba(18, 24, 42, 0.72);
    border-width: 1px;
    border-color: rgba(70, 130, 255, 0.20);
    border-radius: 8px;
    white-space: normal;
}
.rs-preset-btn:hover {
    border-color: rgba(90, 150, 255, 0.50);
}
.rs-preset-btn-active {
    background-color: rgba(22, 55, 155, 0.52);
    border-color: rgba(90, 155, 255, 0.82);
}
.rs-preset-title {
    color: rgb(190, 215, 255);
    font-size: 11px;
    -unity-font-style: bold;
}
.rs-preset-desc {
    color: rgb(128, 155, 190);
    font-size: 10px;
    white-space: normal;
    margin-top: 2px;
}
.rs-user-preset-list {
    margin-top: 3px;
    margin-bottom: 4px;
}
.rs-user-preset-error {
    color: rgb(255, 125, 110);
}
.rs-user-preset-editor {
    margin-top: 6px;
    margin-bottom: 7px;
}
.rs-user-preset-name-label {
    color: rgb(175, 205, 240);
    font-size: 11px;
    margin-left: 2px;
    margin-bottom: 4px;
}
.rs-text-field {
    min-height: 24px;
    margin-top: 3px;
    margin-bottom: 4px;
    color: rgb(175, 205, 240);
}
.rs-text-field > .unity-base-field__input,
.rs-text-field .unity-base-text-field__input {
    background-color: rgba(10, 15, 32, 0.85);
    border-width: 1px;
    border-color: rgba(70, 130, 255, 0.38);
    border-radius: 4px;
    color: rgb(200, 220, 255);
}
.rs-user-preset-name-field {
    min-height: 30px;
    margin-top: 0;
    margin-bottom: 0;
}
.rs-user-preset-actions {
    flex-direction: row;
    align-items: stretch;
    margin-bottom: 6px;
}
.rs-user-preset-action {
    flex-grow: 1;
    flex-basis: 0;
    min-width: 0;
    height: 30px;
    margin-top: 0;
    margin-bottom: 0;
}
.rs-user-preset-action-first {
    margin-right: 3px;
}
.rs-user-preset-action-last {
    margin-left: 3px;
}
.rs-burn-group-list {
    margin-bottom: 8px;
}
.rs-burn-group-row {
    flex-direction: row;
    flex-wrap: wrap;
    margin-top: 4px;
    margin-bottom: 4px;
}
.rs-burn-group-btn {
    min-width: 88px;
    margin-right: 4px;
    margin-bottom: 4px;
    padding: 4px;
    font-size: 10px;
    color: rgb(245, 248, 255);
    background-color: rgba(18, 24, 42, 0.72);
    border-width: 1px;
    border-color: rgba(70, 130, 255, 0.20);
    border-radius: 7px;
}
.rs-burn-group-btn-active {
    color: rgb(255, 255, 255);
    background-color: rgba(22, 55, 155, 0.52);
    border-color: rgba(90, 155, 255, 0.82);
}

/* ══ SCENARIO GRID ══════════════════════════════════════════════════════════ */
.rs-scenario-grid {
    flex-direction: column;
    flex-wrap: nowrap;
    margin-top: 4px;
}
.rs-scenario-card {
    width: auto;
    margin-bottom: 5px;
    padding: 7px;
    background-color: rgba(18, 24, 42, 0.72);
    border-width: 1px;
    border-color: rgba(70, 130, 255, 0.20);
    border-radius: 7px;
    overflow: visible;
}
.rs-scenario-card:hover  { border-color: rgba(90, 150, 255, 0.50); }
.rs-scenario-card-active {
    background-color: rgba(22, 55, 155, 0.52);
    border-color: rgba(255, 185, 60, 0.80);   /* amber highlight for Scenario tab */
}
.rs-scenario-title {
    color: rgb(175, 205, 255);
    font-size: 12px;
    -unity-font-style: bold;
    margin-bottom: 3px;
    overflow: visible;
    white-space: normal;
}
.rs-scenario-desc {
    color: rgb(110, 140, 170);
    font-size: 10px;
    white-space: normal;
    overflow: visible;
}

/* == REWARD PRESETS ======================================================== */
.rs-reward-preset-row {
    flex-direction: row;
    flex-wrap: wrap;
    margin-top: 4px;
    margin-bottom: 5px;
}
.rs-reward-preset-btn {
    min-width: 82px;
    height: 23px;
    margin: 2px;
    padding-left: 6px;
    padding-right: 6px;
    background-color: rgba(18, 24, 42, 0.72);
    border-width: 1px;
    border-color: rgba(70, 130, 255, 0.20);
    border-radius: 5px;
    color: rgb(130, 160, 205);
    font-size: 10px;
    overflow: hidden;
}
.rs-reward-preset-btn:hover {
    border-color: rgba(90, 150, 255, 0.45);
}
.rs-reward-preset-btn-active {
    background-color: rgba(120, 80, 20, 0.55);
    border-color: rgba(255, 185, 60, 0.75);
    color: rgb(255, 205, 120);
}

/* == COMPLETE TRAINING OBJECTIVE =========================================== */
.rs-objective-scenario {
    color: rgb(255, 195, 120);
    font-size: 12px;
    -unity-font-style: bold;
    margin-bottom: 3px;
}
.rs-objective-validation {
    margin-top: 6px;
    margin-bottom: 5px;
    padding: 6px 7px;
    border-width: 1px;
    border-radius: 5px;
    white-space: normal;
    font-size: 9px;
}
.rs-objective-validation-ok {
    color: rgb(105, 210, 145);
    background-color: rgba(20, 105, 65, 0.18);
    border-color: rgba(65, 190, 120, 0.35);
}
.rs-objective-validation-warning {
    color: rgb(245, 190, 90);
    background-color: rgba(145, 90, 20, 0.20);
    border-color: rgba(230, 155, 55, 0.42);
}
.rs-objective-validation-error {
    color: rgb(255, 125, 110);
    background-color: rgba(150, 40, 35, 0.22);
    border-color: rgba(235, 85, 75, 0.52);
}
.rs-objective-preset-status-row {
    flex-direction: row;
    align-items: center;
    margin-top: 2px;
    margin-bottom: 4px;
}
.rs-objective-preset-status {
    flex-grow: 1;
    color: rgb(150, 180, 215);
    font-size: 10px;
}
.rs-objective-preset-custom { color: rgb(245, 180, 85); }
.rs-objective-reset-btn {
    height: 22px;
    padding-left: 7px;
    padding-right: 7px;
    background-color: rgba(65, 45, 25, 0.75);
    border-width: 1px;
    border-color: rgba(210, 135, 55, 0.45);
    border-radius: 4px;
    color: rgb(230, 175, 105);
    font-size: 9px;
}
.rs-objective-reset-btn:hover { border-color: rgba(245, 175, 85, 0.75); }

.rs-termination-group-heading {
    color: rgb(140, 160, 195);
    font-size: 9px;
    -unity-font-style: bold;
    letter-spacing: 0.8px;
    margin-top: 9px;
    margin-bottom: 4px;
}
.rs-termination-rule {
    margin-bottom: 6px;
    padding: 6px;
    background-color: rgba(15, 20, 36, 0.76);
    border-width: 1px;
    border-left-width: 3px;
    border-color: rgba(85, 110, 150, 0.28);
    border-radius: 6px;
}
.rs-termination-success { border-left-color: rgba(65, 195, 120, 0.72); }
.rs-termination-outcome { border-left-color: rgba(80, 155, 235, 0.72); }
.rs-termination-failure { border-left-color: rgba(225, 85, 75, 0.72); }
.rs-termination-limit   { border-left-color: rgba(220, 150, 60, 0.68); }
.rs-termination-rule-disabled {
    background-color: rgba(13, 17, 29, 0.60);
    border-top-color: rgba(70, 85, 115, 0.20);
    border-right-color: rgba(70, 85, 115, 0.20);
    border-bottom-color: rgba(70, 85, 115, 0.20);
}
.rs-termination-rule-header {
    flex-direction: row;
    align-items: center;
    min-height: 23px;
}
.rs-termination-rule-toggle {
    flex-grow: 1;
    color: rgb(180, 205, 235);
    font-size: 10px;
}
.rs-termination-kind {
    width: 50px;
    margin-left: 4px;
    color: rgb(125, 150, 185);
    font-size: 8px;
    -unity-font-style: bold;
    -unity-text-align: middle-right;
}
.rs-termination-reset {
    width: 39px;
    height: 20px;
    margin-left: 5px;
    padding-left: 2px;
    padding-right: 2px;
    background-color: rgba(28, 38, 58, 0.82);
    border-width: 1px;
    border-color: rgba(90, 120, 165, 0.34);
    border-radius: 4px;
    color: rgb(140, 165, 198);
    font-size: 8px;
}
.rs-termination-description {
    color: rgb(110, 135, 165);
    font-size: 9px;
    white-space: normal;
    margin-top: 2px;
}
.rs-termination-outcomes {
    color: rgb(135, 165, 205);
    font-size: 8px;
    white-space: normal;
    margin-top: 2px;
}
.rs-termination-thresholds { margin-top: 5px; }
.rs-termination-shared-note {
    color: rgb(125, 145, 175);
    font-size: 8px;
    -unity-font-style: italic;
    white-space: normal;
    margin-top: 3px;
}
.rs-termination-boolean > .rs-control-description {
    margin-left: 154px;
    margin-right: 4px;
}

/* ══ WEATHER BUTTONS ════════════════════════════════════════════════════════ */
.rs-btn-group { flex-direction: row; flex-wrap: wrap; margin-bottom: 5px; }
.rs-weather-btn {
    flex-grow: 1;
    height: 24px;
    background-color: rgba(18, 24, 42, 0.72);
    border-width: 1px;
    border-color: rgba(70, 130, 255, 0.20);
    border-radius: 5px;
    color: rgb(130, 160, 205);
    font-size: 11px;
    margin: 2px;
    overflow: hidden;
}
.rs-weather-btn:hover  { border-color: rgba(90, 150, 255, 0.45); }
.rs-weather-btn-active {
    background-color: rgba(15, 110, 75, 0.55);  /* green to match Env tab */
    border-color: rgba(55, 200, 135, 0.70);
    color: rgb(80, 220, 160);
}

/* ══ INFERENCE RUN GRID ═════════════════════════════════════════════════════ */
.rs-inference-grid {
    flex-direction: column;
    margin-top: 4px;
    margin-bottom: 6px;
}
.rs-inference-card {
    padding: 7px 10px;
    margin-bottom: 4px;
    background-color: rgba(18, 24, 42, 0.72);
    border-width: 1px;
    border-color: rgba(70, 130, 255, 0.20);
    border-radius: 7px;
    overflow: hidden;
}
.rs-inference-card:hover {
    border-color: rgba(90, 150, 255, 0.50);
    background-color: rgba(25, 36, 65, 0.82);
}
.rs-inference-card-active {
    background-color: rgba(22, 55, 155, 0.50);
    border-color: rgba(90, 155, 255, 0.80);
}
.rs-inference-card-name {
    color: rgb(185, 210, 255);
    font-size: 12px;
    -unity-font-style: bold;
    margin-bottom: 2px;
    overflow: hidden;
    white-space: nowrap;
}
.rs-inference-card-scenario {
    color: rgb(100, 155, 210);
    font-size: 10px;
    overflow: hidden;
    white-space: nowrap;
}

/* ══ EMPTY STATE ════════════════════════════════════════════════════════════ */
.rs-empty-state {
    color: rgb(88, 115, 150);
    font-size: 11px;
    white-space: normal;
    margin: 12px 8px;
    -unity-text-align: middle-center;
}

/* ══ TOGGLE BASELINE FIX ════════════════════════════════════════════════════
   Ensure Unity Toggle label text is readable.                               */
.unity-toggle__label { color: rgb(155, 180, 210); font-size: 11px; }
.unity-toggle__checkmark {
    -unity-background-image-tint-color: rgb(80, 185, 255);
}

/* ══ SLIDER BASELINE FIX ════════════════════════════════════════════════════ */
.unity-slider__input { border-width: 0; background-color: transparent; }
.unity-base-slider__drag-container {
    margin-left: 0;
    margin-right: 0;
    height: 12px;           /* explicit height = same as dragger */
}
.unity-base-slider__tracker {
    background-color: rgba(40, 65, 130, 0.55);
    border-radius: 2px;
    height: 4px;
    margin-top: 4px;        /* (12 - 4) / 2 */
}
.unity-base-slider__dragger {
    background-color: rgb(80, 165, 255);
    border-radius: 50%;
    width: 12px;
    height: 12px;
    border-width: 0;
    margin-top: 0;
}
.unity-base-slider__dragger:hover { background-color: rgb(110, 190, 255); }

/* ══ SCROLL VIEW THEMING ════════════════════════════════════════════════════ */
.unity-scroll-view__vertical-scroller { width: 6px; }
.unity-scroller__slider .unity-base-slider__dragger {
    background-color: rgba(70, 130, 255, 0.40);
    border-radius: 3px;
    width: 6px;
    top: 0;
}
.unity-scroller__slider .unity-base-slider__dragger:hover {
    background-color: rgba(90, 155, 255, 0.65);
}
.unity-scroller__slider .unity-base-slider__tracker {
    background-color: rgba(30, 40, 70, 0.50);
}
